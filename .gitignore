__pycache__/
*.pyc
.DS_Store
.venv/

# Human annotation results live in Google Drive (the crowdsource Responses sheet).
# We publish the code and the LLM pilot ratings, but NOT the human responses.
# Never commit anything downloaded/exported from the annotation form.
results/human_ratings.csv
results/human_ratings*.csv
results/responses*.csv
crowdsource/responses*.csv
crowdsource/*.exported.csv

# clasp deploy artifacts (the web-app project lives outside this repo)
.clasp.json
.clasprc.json

# Human annotation data — NOT released.
# Raw export contains annotator names/emails (PII); the de-identified panel file
# is also withheld per project policy. validation/RESULTS.md publishes the metrics,
# and validation/compute_validation.py regenerates them from these inputs.
data/human_annotations_raw.json
data/human_annotations_tidy.csv
validation/audit_scores_200.csv
